name: entity_sync_generator
description: Code generator for the entity_sync package.
version: 0.0.1
homepage:

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  entity_sync:
    git:
      url: https://github.com/qBozo/entity_sync
      ref: dart3-fixes
  analyzer: ">=4.0.0 <7.0.0"
  build: ^2.4.1
  source_gen: ^1.2.2

dev_dependencies:
  build_runner: ^2.4.8
  source_gen: ^1.2.2

dependency_overrides:
  file: ^7.0.1
